pipeline {
    agent any
    tools {
        maven 'Maven 3.6.3'
    }

    stages{
        stage ('Build') {
            steps {
                echo 'YOU SUCK!'
                sh 'java --version'
                sh 'mvn --version'
            }
        }
    }

}